Lab 9.1 One To Many

Yêu cầu 1: Tạo bảng dữ liệu	2
Yêu cầu 2: Insert dữ liệu vào các bảng students và papers	2
Yêu cầu 3: Viết truy vấn tất cả bản ghi (các hàng dữ liệu) của bảng students và papers:	3
Yêu cầu 4: Sử dụng phép các Join hợp lý của bảng students và papers để tạo được kết quả như dưới đây	3


Yêu cầu 1: Tạo bảng dữ liệu

Trong cơ sở dữ liệu book_shop, học viên cần tạo hai bảng dữ liệu như sau:
Bảng students có các trường:
Id: Là khoá chính, có kiểu dữ liệu int và tự động tăng
first_name: Có kiểu dữ liệu varchar(150), not null
Bảng papers có các trường:
student_id: chính là id của student (student sẽ làm foreign key của bảng paper)
title: Có kiểu dữ liệu varchart(150)
grade: Có kiểu dữ liệu int.

CREATE TABLE students(Id INT AUTO_INCREMENT PRIMARY KEY, first_name VARCHAR(150) NOT NULL);
CREATE TABLE papers(student_id INT , title VARCHAR(150), grade INT, FOREIGN KEY (student_id) REFERENCES students(Id));


Yêu cầu 2: Insert dữ liệu vào các bảng students và papers

Học viên thực hiện các lệnh dưới đây:
INSERT INTO students (first_name) VALUES 
('Caleb'), ('Samantha'), ('Raj'), ('Carlos'), ('Lisa');

INSERT INTO papers (student_id, title, grade ) VALUES
(1, 'My First Book Report', 60),
(1, 'My Second Book Report', 75),
(2, 'Russian Lit Through The Ages', 94),
(2, 'De Montaigne and The Art of The Essay', 98),
(4, 'Borges and Magical Realism', 89);



Yêu cầu 3: Viết truy vấn tất cả bản ghi (các hàng dữ liệu) của bảng students và papers:

Kết quả như sau:
Bảng students:

Bảng papers:


Yêu cầu 4: Sử dụng phép các Join hợp lý của bảng students và papers để tạo được kết quả theo mẫu

Kết quả 1:
SELECT first_name,title,grade FROM students LEFT JOIN papers ON Id = student_id ORDER BY Id;

Kết quả 2:
SELECT  first_name,
        IFNULL(title,"MISSING"),
        IFNULL(grade,0) 
    FROM students 
    LEFT JOIN papers 
    ON Id = student_id 
    ORDER BY Id;

Kết quả 3:
SELECT first_name,IFNULL(AVG(grade),0) AS average  FROM students LEFT JOIN papers ON Id = student_id GROUP BY first_name ORDER BY average DESC;

Kết quả 4:
SELECT first_name,
    IFNULL(AVG(grade),0) AS average,
    CASE 
        WHEN AVG(grade) >= 80 THEN 'PASSING'
        ELSE 'FAILING'
        END AS passing_status
FROM students 
LEFT JOIN papers ON Id = student_id 
GROUP BY first_name 
ORDER BY average DESC;
